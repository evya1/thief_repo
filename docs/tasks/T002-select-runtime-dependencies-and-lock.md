---
id: T002
status: ready
priority: P0
task_type: foundation
component: system
optional: false
implements:
  - NET-001
  - QR-014
context_files:
  - docs/PRD.md
  - docs/PLAN.md
read_set:
  - config/repo_quality.toml
depends_on: []
gates:
  - id: PLANQ-002
    kind: decision
    scope: dependency_lock
    blocks: criterion
parallel_safe: true
claimed_by:
claim_expires_at:
write_set:
  - pyproject.toml
  - uv.lock
  - .github/workflows/ci.yml
  - tests/test_runtime_dependencies.py
risk: medium
---

# T002 — Select Runtime Dependencies And Lock

## Expected outcome

The repository has a reviewed initial foundation dependency baseline — Python baseline, FastMCP direct dependency, the existing test/quality dependencies, and the approved uv bootstrap/version — plus a reproducible initial uv lock, without speculative runtime packages.

## Requirements implemented

- `NET-001`
- `QR-014`

## Relevant context

This planning scaffold intentionally omits uv.lock because runtime libraries and compatible versions have not yet been approved. Quality-only dependencies already live in pyproject.toml.

This task selects the *initial* foundation dependency baseline only, per the narrowed `PLANQ-002`: the Python baseline, the FastMCP direct dependency, the existing test/quality dependencies, and the already-approved uv bootstrap/version policy. It does not select the GUI toolkit, which `PLANQ-007` owns, and it does not select the Gmail sender library, which `PLANQ-005` owns. Do not install a package in this task merely because a later component might need it.

`pyproject.toml` and `uv.lock` are repository-global, whole-environment integration artifacts with serialized mutation ownership. T002 owns their initial creation. A later component task does not automatically inherit that ownership: if a human-approved implementation decision later requires a dependency change, the component worker surfaces the requirement to the orchestrator, who explicitly assigns serialized dependency-integration work before either file is edited. A component worker must not silently widen its own write set to reach these files.

The Python baseline that CI installs is read from `config/repo_quality.toml`, which this task reads but does not own; changing that value is outside this write set and needs an orchestrator-approved write-set change.

This task owns `.github/workflows/ci.yml`, whose uv bootstrap was repaired ahead of it under `docs/decisions/ADR-002-ci-uv-bootstrap.md`: CI installs a pinned, checksum-verified uv release instead of a third-party marketplace action. Inherit that decision rather than reopening it. When the approved lock lands, change `uv sync --all-groups` to `uv sync --locked --all-groups` and leave the bootstrap step unchanged.

## Gates

- `PLANQ-002` (`decision`, `blocks: criterion`) — the task may be claimed and implemented now; only the acceptance criterion scoped `dependency_lock` waits.

## Constraints

- Do not edit the canonical PRD.
- Do not silently redefine the repository PLAN or task dependencies.
- Do not widen scope or treat a derived design decision as a source requirement.
- Edit only the declared write set; request an orchestrator-approved change before crossing it.
- Escalate requirement conflicts, missing official inputs, and newly discovered work.

## Acceptance criteria

- [ ] Each selected runtime dependency has a requirement/design justification and compatibility note.
- [ ] pyproject.toml is the sole dependency declaration and contains no requirements.txt migration residue.
- [ ] uv.lock is generated by uv, committed, and validates with uv sync --locked --all-groups, per the approved PLANQ-002 dependency selection. The lock is complete for the dependency decisions approved at T002 time; it is not required to cover dependencies belonging to later decisions such as PLANQ-005 and PLANQ-007. `{#dependency_lock}`
- [ ] CI uses the committed lock and the same Ruff, pytest, and repository-gate commands as pre-commit.
- [ ] An import smoke test covers the approved runtime libraries without implementing game behavior.
- [ ] No GUI or Gmail package is selected in this task; both remain owned by PLANQ-007 and PLANQ-005 respectively.

## Verification

- `uv lock --check`
- `uv sync --locked --all-groups`
- `uv run ruff check .`
- `uv run pytest`
- `uv run python scripts/run_quality_gates.py`

## Implementation plan

To be completed immediately before execution.

## Handoff contract

Report files changed, tests executed, exact test results, decisions made, deviations, blockers, and newly discovered work. Include command output or artifact paths sufficient for the orchestrator to validate every acceptance criterion.

## Result and evidence
